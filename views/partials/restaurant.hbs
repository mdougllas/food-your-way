<!-- This file is just one partials.
It's content is being required y dashboard.hbs -->
<div class="card card-resp">
  <img class="card-img-top" src="{{this.image_url}}" alt="{{this.name}}">
  <div class="card-body">
    <h5 class="card-title">{{this.name}}</h5>
    <p class="card-text">{{this.address}}<br>{{this.city}}<br>{{this.phone}}</p>
    <a class="btn btn-primary" href="{{this.reserve_url}}">Book Now</a>
    <a class="btn btn-primary" href="/edit-restaurant?id={{this.id}}">Edit Restaurant</a><br><br>
    <a class="btn btn-primary" href="/delete-restaurant?id={{this.id}}">Delete</a>
  </div>
</div>